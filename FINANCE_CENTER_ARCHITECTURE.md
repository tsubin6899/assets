# 個人財務中心架構

## 主要入口

- `finance-center.html`：原生單頁財務中心，提供首頁、日常記帳、帳戶與負債、投資資產、分析與報表。
- `finance-center-routes.js`：主要領域與分頁定義，避免分頁結構散落在畫面程式中。
- `finance-core.js`：唯一財務資料來源，負責舊資料遷移、財務事件、餘額、持股、市值、分析、備份與所有寫入操作。
- `finance-market.js`：讀取與觸發股價、匯率及估值更新，再交由 `finance-core.js` 統一套用。
- `accounting-app.html`、`personal-assets-dashboard.html`：僅保留作為舊網址與資料格式相容頁；主介面已不再導向這兩個頁面。

## 四階段整併完成範圍

1. 日常核心：收支 CRUD 與篩選、帳戶編輯、轉帳、信用卡帳單、固定收支、分類預算。
2. 進階帳務：信用卡分期、帳戶盤點與差額調整、信用卡帳單核對、月結快照。
3. 投資與分析：持倉、買賣、股息 CRUD、現金帳戶連動、資產配置與淨資產快照。
4. 單一入口：統一導航、資料契約、雲端同步、備份／還原及離線快取；主介面移除舊系統入口。

## 相容資料來源

- 記帳：`personal-accounting-tsubin-v1`
- 資產：`personal-assets-dashboard-tsubin-v2`
- 統一索引：`tsubin-personal-finance-center-v3`
- 還原點：`tsubin-personal-finance-backups-v1`

舊資料仍是可讀寫的相容來源；財務中心不要求使用者重新匯入或重新建檔。

## 統一財務事件

- `income`、`expense`：一般收入與支出。
- `transfer`：帳戶間移轉，不納入收入或支出。
- `investment_buy`、`investment_sell`：投資現金流，不納入一般消費預算。
- `dividend`：投資收入，可連動入款帳戶。

信用卡消費增加負債；繳款以轉帳降低負債，不重複計算支出。

帳戶餘額、月份收支、信用卡負債、投資現金流、持股股數與總資產不可由畫面自行保存結果；畫面只讀取 `FinanceCore.insights()` 的推導結果。

## 市場資料與匯率

- 新版財務中心會自動讀取 `latest-prices.json`、`latest-rates.json` 與 `latest-valuations.json`。
- 使用者可從頂端或「投資資產／資產配置」頁手動更新行情。
- 更新請求沿用既有 `/api/update-prices` 與 `MARKET_DATA_UPDATE_ENDPOINT` 流程。
- 股價保存於 `assets.marketPrices`，不再依賴舊版手動持倉是否存在；只有淨股數大於零的交易持股才計入市值。
- 匯率保存於 `assets.fxRates`，外幣帳戶、外幣交易與美股市值皆使用同一匯率折算台幣。
- `assets.marketDataMeta` 保存資料時間與來源；重複讀取同一版本時不重複寫入。
- 市場資料更新不建立大量自動備份，但仍會觸發既有雲端同步。
- 外幣帳戶以原幣保存餘額，帳戶卡片另顯示目前匯率與約當台幣；交易可選 TWD 或 USD，跨幣別時先換算為帳戶幣別入帳。

## 快速記帳與信用卡

- 快速記帳的「分類」與「項目」採連動選單；新增過的分類／項目會回寫到統一資料層供下次選用。
- 收支紀錄可保存為常用範本，重新套用時會帶入類型、金額、分類、項目、帳戶、幣別及刷卡地區。
- 信用卡交易分為國內與國外刷卡。只有「信用卡＋國外刷卡＋支出」會自動建立原交易 1.5% 的國外刷卡手續費。
- 自動手續費以 `derivedFromEntryId` 連結原交易；原交易修改或刪除時，手續費同步重算或移除，避免重複入帳。
- 收支 CSV 匯入支援中英文欄名、TWD／USD 與國內外欄位，並以日期、類型、金額、帳戶、商家與項目判斷重複資料。

## 財務中心原生檢視

- 日常記帳提供月份行事曆，可切換月份並依日期檢視、編輯或刪除當日收支。
- 固定收入與固定支出採綠色／珊瑚紅視覺語意，避免只依文字判斷。
- 帳戶卡片依現金、銀行、外幣、行動支付、信用卡與其他類型呈現；信用卡待繳金額一律使用紅字正數。
- 投資頁沿用財務中心統一內容寬度，長交易資訊不可撐破版面。
- 投資賣出明細以加權平均成本計算本次成本、淨賣出收入、已實現盈虧及報酬率；不同幣別的頁首彙總先換算為 TWD。
- 手機版採雙層懸浮導覽：底層為五個主要領域，上層為可觸控左右滑動的子分頁列；切換分頁時目前項目會自動置中。

## 投資持倉來源

- 有交易紀錄的台股、美股、台幣基金與美元基金，以累計買入減累計賣出的淨股數／單位為唯一持倉依據。
- 淨股數為 0 的標的保留在「已出清」清單，但不再計入股票市值與總資產。
- 舊 `tw`／`us` 手動持倉若與交易標的相同，只提供現價與核對資訊，不重複計算股數。
- 尚無交易紀錄的手動持倉繼續有效；同步舊持倉時會先保存原股數至 `legacySharesBeforeTradeSync`。

## 資料安全

- 每次主要寫入、匯入與還原前建立還原點，最多保留 12 份。
- 自動還原點不保存大型發票影像，完整 JSON 匯出仍保留原始資料。
- 雲端資料使用既有 Supabase 使用者資料列與 RLS；載入較舊雲端版本前會提示衝突。
- Email OTP 登入共用 `tsubin-assets-auth` 工作階段；手機使用登入視窗，公開設定缺漏時回退至頁面公開設定。
- 登入回呼憑證會先交由 Supabase 驗證，再恢復 App 分頁路由；頁面回到前景時會主動重新驗證工作階段。
- 舊版預設 Supabase 工作階段會安全搬到共用工作階段鍵，成功後以綠色「✓ 已登入」徽章顯示。
- 離開頁面或閒置五分鐘會自動隱藏金額。

## 後續開發原則

新增財務功能時，先在 `finance-core.js` 定義資料與事件，再由頁面渲染。避免在不同頁面各自重新計算餘額、淨資產或現金流。

分頁增刪先修改 `finance-center-routes.js`；外部行情來源與更新流程先修改 `finance-market.js`。`finance-center.html` 只負責畫面組合與互動。

## 固定收支與未實現項目

- 固定收支規則由 `finance-core.js` 統一計算日期；日期尚未到達時只提供行事曆預告，標示為「未實現」。
- 未來日期的收入與支出不列入帳戶餘額、首頁月統計、明細或月結。
- 到期當天載入系統時，固定收支才會建立正式收支紀錄，並以 `recurringId + scheduledDate` 防止重複入帳。
- 手機底部主選單使用 `inset: auto 0 0` 固定貼齊視窗底部，浮動分頁列固定於其正上方；兩者皆不使用 `100vw`，避免動態視窗寬度造成橫向溢出。分頁列保留水平觸控捲動，不顯示提示文字。

導覽事件中的 `data-domain` 只保留給主要導覽按鈕；頁面與子分頁若只需套用區域樣式，必須使用 `data-section`，避免點擊內容時把路由重設到第一個分頁。
